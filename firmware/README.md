# firmware/

Out of scope for this build pass. Will hold the ESP8266 Arduino firmware
(`esp8266/config.h`, `wifi.cpp`, `mqtt.cpp`, `relay.cpp`, `heartbeat.cpp`, `main.ino`) per
specification.md section 6, implementing the MQTT topics/payloads defined in section 7.
